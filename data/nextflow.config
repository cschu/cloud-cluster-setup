executor {
    name = 'slurm'
    queueSize = 3    // 3 tasks in parallel
}

executor {
    name = 'local'
}

/*profiles {

    standard {
        process.executor = 'local'
    }

    cluster {
	process.executor.queueSize = 3
	process.executor = 'slurm'
    }

}*/

process {
	executor = 'slurm'
}
